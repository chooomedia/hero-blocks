{% block sw_extension_card_base_info %}
    {% parent %}
    
    {# Description im Info-Bereich (nur für HeroBlocks) - Direkt unter dem Namen #}
    {# WICHTIG: Prüfe verschiedene Extension-Identifikatoren (name, composerName, label) #}
    <p v-if="(extension.name === 'HeroBlocks' || extension.composerName === 'horex/hero-blocks' || extension.label === 'Hero Blocks') && getExtensionDescription(extension)" class="sw-extension-card-base__description-text">
        {{ getExtensionDescription(extension) }}
    </p>
{% endblock %}

