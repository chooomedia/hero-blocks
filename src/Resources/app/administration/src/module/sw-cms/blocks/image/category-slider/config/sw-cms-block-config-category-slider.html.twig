{% block sw_cms_block_config_category_slider %}
<div class="sw-cms-block-config-category-slider" v-if="block">
    {# 
        WICHTIG: Block-Config enthält NUR Standard-Settings (Margins, Sizing)
        ALLE Custom Settings (Category, Image Count, Navigation, etc.) sind im Element-Modal!
        
        Dokumentation:
        - Standard Block-Settings: Margins, Sizing Mode (werden von Shopware bereitgestellt)
        - Custom Settings: Werden im Element-Modal konfiguriert (siehe sw-cms-el-config-category-slider)
    #}
    
    {# WICHTIG: Info-Banner - alle Settings im Element-Modal #}
    {% block sw_cms_block_config_category_slider_info %}
    <mt-banner variant="info" class="sw-cms-block-config-category-slider__info">
        {{ $tc('sw-cms.blocks.heroBlocks.categorySlider.config.info') }}
    </mt-banner>
    {% endblock %}
</div>
{% endblock %}

