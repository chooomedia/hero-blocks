/**
 * Hero Slider verwendet BaseSliderPlugin direkt
 * Kein zusätzliches JavaScript-Plugin nötig
 * Animation wird über CSS gehandhabt (gemäß Shopware Best Practices)
 * 
 * BaseSliderPlugin wird automatisch für [data-base-slider="true"] initialisiert
 */

