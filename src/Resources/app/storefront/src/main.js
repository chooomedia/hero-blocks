/**
 * HeroBlocks Plugin - Storefront Entry Point
 * 
 * WICHTIG: Plugin-Styles werden über theme.json geladen (@Plugins in Theme)
 * main.js ist nur für JavaScript-Plugins (falls nötig)
 * 
 * BaseSliderPlugin wird automatisch für [data-base-slider="true"] initialisiert
 */

